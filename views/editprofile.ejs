<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <!-- Profile Section -->
    <div class="flex justify-center mt-10">
        <div class="w-2/3 bg-white rounded-xl shadow-lg p-8">
            
            <!-- Edit Profile Form -->
            <h3 class="text-xl font-semibold mb-4">Edit Profile</h3>
            <form action="/user/edit/<%=user._id%>" enctype="multipart/form-data" method="post">
                <input type="password" name="opassword" placeholder="Current Password" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <input type="text" name="username" value="<%= user.username %>" placeholder="Username" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <input type="email" name="email" value="<%= user.email %>" placeholder="Email" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <input type="file" name="profilePic" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <input type="password" name="password" placeholder="New Password" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <input type="password" name="cpassword" placeholder="Confirm Password" class="mb-4 px-4 py-2 w-full rounded-lg border border-gray-300">
                <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-lg">Save Changes</button>
            </form>
            
            

            <!-- Adding Space Before the Back Button -->
            <div class="mt-6">
                <a href="/user/profile" class="bg-red-500 text-white px-6 py-2 rounded-lg">Back</a>
            </div>

            <%if(error){%>
                <h3 class="text-md font-semibold mb-4 pt-4"><%=error%></h3>
            <%}%>

        </div>
    </div>

</body>
</html>
